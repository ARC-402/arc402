# 2026-03-19

- Shipped first ARC-402 endpoint CLI scaffold under `arc402 endpoint`.
- Added persisted endpoint config at `~/.arc402/endpoint.json` so canonical public identity is stored separately from daemon/OpenShell state.
- `arc402 endpoint init` now scaffolds `agentname.arc402.xyz` + local ingress target using launch-default host-managed Cloudflare Tunnel language.
- `arc402 endpoint status` and `arc402 endpoint doctor` now distinguish runtime sandbox health from ingress/tunnel health instead of blurring them.
- `arc402 endpoint claim <agentname> --tunnel-target <https://...>` now claims the hostname and locks local config, but AgentRegistry endpoint update remains a separate explicit step for now.
- Endpoint hardening follow-up shipped: status/doctor now include DNS resolution proof, wallet-binding checks, and AgentRegistry public-endpoint parity/mismatch diagnostics. Remaining endpoint work is deeper tunnel introspection plus any richer public-route verification that depends on external infrastructure semantics.
- Publish-surface readiness sweep cleaned the launch-facing docs stack: root README now points to the launch checklist and canonical endpoint path, CLI + skill docs align on endpoint-before-registration sequencing, `reference/README.md` no longer reads like a stale pre-mainnet entry point, and the launch checklist was rewritten around the locked publish order (public surfaces first, then MacBook proof, then publish).

- Late-night framing correction: launch/public surfaces must present ARC-402 as one product for agent-to-agent hiring with governed sandboxed execution. The machine story is a dedicated commerce sandbox / governed workroom for hired work; OpenShell is the runtime safety layer underneath, not the front-facing noun. OpenClaw users add ARC-402 for hired work rather than migrating their full environment.
