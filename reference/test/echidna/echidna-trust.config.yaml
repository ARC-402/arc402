# Echidna configuration for TrustRegistryV3 invariant testing
# Run: echidna . --contract TrustRegistryV3Invariants --config test/echidna/echidna-trust.config.yaml

testMode: "property"
testLimit: 10000
seqLen: 50

contractAddr: "0x00a329c0648769A73afAc7F9381E08FB43dBEA72"
deployer: "0x30000"

sender:
  - "0x10000"   # VOUCHER1 / W1
  - "0x20000"   # VOUCHER2 / W2
  - "0x30000"   # Owner / authorized updater

solcArgs: "--solc-version 0.8.24"
